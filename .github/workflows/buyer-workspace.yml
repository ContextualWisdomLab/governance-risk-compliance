name: Buyer Workspace

on:
  pull_request:
    branches: [develop]
    paths:
      - 'apps/grc-workspace/**'
      - '.storybook/**'
      - '.gitignore'
      - 'mise.toml'
      - 'package.json'
      - 'package-lock.json'
      - 'playwright.config.mjs'
      - 'tests/test_buyer_workspace_design_contract.py'
      - 'tests/buyer-workspace.spec.mjs'
      - 'docs/adr/0012-grc-buyer-workspace-design-authority.md'
      - 'docs/product/grc-buyer-workspace-*.md'
      - '.github/workflows/buyer-workspace.yml'

permissions:
  contents: read

jobs:
  storybook:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Harden runner
        uses: step-security/harden-runner@b09bb98e06d4d774595224525879c09bc6e98c40 # v2.20.1
        with:
          egress-policy: audit
      - name: Checkout exact pull-request head
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          persist-credentials: false
      - name: Verify exact head
        shell: bash
        run: test "$(git rev-parse HEAD)" = "${{ github.event.pull_request.head.sha }}"
      - name: Setup Node
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6.5.0
        with:
          node-version: '24.18.0'
          package-manager-cache: false
      - name: Enable the checked-in package manager
        run: corepack enable
      - name: Install the hash-locked frontend
        run: corepack npm ci --ignore-scripts --no-audit --no-fund
      - name: Build Storybook
        run: npm run build-storybook
      - name: Install the pinned browser
        run: corepack npm exec -- playwright install --with-deps chromium
      - name: Run buyer-workspace browser checks
        run: npm run test:buyer-workspace
      - name: Verify repository remains clean
        run: |
          git diff --check
          test -z "$(git status --porcelain)"
